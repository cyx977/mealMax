import 'package:flutter/material.dart';

class FavouriteScreen extends StatelessWidget {
  static final route = "/favourite";
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("this is the fav thing"),
    );
  }
}
